#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints a string
 * @str: The string to print
 * Return: void
 */

int main(void)

{
	char *str;

	while (*str != '\0')
	{
		_putchar(*str++);
	}
		_putchar('\n');
	return (0)
}
